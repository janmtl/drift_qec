.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install drift_qec

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv drift_qec
    $ pip install drift_qec
